# Synthetic Word Binary fixture

`sample_legacy.doc` is the synthetic `sample_strict.docx` converted by LibreOffice 26.2.2.2 using the MS Word 97-2003 export filter. It contains headings, paragraphs, and a table; it has no personal or third-party content. File and source hashes are in `legacy_doc.provenance.json`.
